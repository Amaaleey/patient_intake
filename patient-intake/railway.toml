[build]
builder = "nixpacks"

[deploy]
startCommand = "cd backend && /app/venv/bin/python start_mcp_servers.py & /app/venv/bin/uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
