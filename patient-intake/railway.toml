[build]
builder = "nixpacks"

[deploy]
startCommand = "cd backend && source .venv/bin/activate && python start_mcp_servers.py & uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"