[build]
builder = "nixpacks"

[deploy]
startCommand = "/app/venv/bin/python patient-intake/backend/start_mcp_servers.py & cd patient-intake/backend && /app/venv/bin/uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
